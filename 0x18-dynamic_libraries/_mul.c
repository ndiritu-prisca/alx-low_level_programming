#include "main.h"

/**
  *mul - a function that multiplies two integers and returns the result.
  *
  *@x: first integer
  *@y: second integer
  *
  *Return: x * y
  */

int mul(int x, int y)
{
	return (x * y);
}
